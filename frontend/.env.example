# Copy this file to `.env.local` (or set these variables in your hosting
# platform's dashboard, e.g. Vercel) to control where the frontend sends API
# requests.

# Production backend (Render). Do NOT include a trailing slash.
NEXT_PUBLIC_API_URL=https://stock-portfolio1.onrender.com

# Local development (only if you are running the backend locally on :8000).
# NEXT_PUBLIC_API_URL=http://localhost:8000
