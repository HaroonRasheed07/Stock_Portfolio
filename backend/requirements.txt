# Backend dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
python-multipart==0.0.20

# Data & Analysis
# yfinance>=1.0 uses curl_cffi browser impersonation, which Yahoo
# requires to serve market data to datacenter IPs (e.g. Render/AWS).
# v0.2.x uses plain `requests` which Yahoo actively blocks (empty
# responses / "Expecting value: line 1 column 1" JSON errors).
yfinance>=1.0.0
curl_cffi>=0.15.0
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.6.1
ta==0.11.0
textblob==0.18.0
vaderSentiment==3.3.2
feedparser==6.0.11

# HTTP & async
httpx==0.28.1
aiofiles==24.1.0

# Background tasks
apscheduler==3.10.4

# Factor analysis (optional)
alphalens-reloaded==0.4.5

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
